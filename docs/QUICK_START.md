# 🚀 Быстрый старт проекта Rem-Maxvich-Stroi

## Текущее состояние ✅

Проект успешно запущен и работает на **http://localhost:3000**

### Что уже готово:
- ✅ Базовая структура проекта Next.js
- ✅ Настроена цветовая схема Tailwind CSS
- ✅ Шапка сайта (Header) с мобильным меню
- ✅ Подвал (Footer) с контактами и соцсетями
- ✅ Hero секция главной страницы
- ✅ Секция преимуществ
- ✅ Базовые UI компоненты (Button, Card, Container, Section)

---

## 📝 Что нужно сделать дальше

### 1. Обновить контактные данные
Откройте файл [src/data/constants.ts](../src/data/constants.ts) и замените заглушки:

```typescript
export const CONTACT_INFO = {
  phone: "+7 (XXX) XXX-XX-XX",        // ← ЗАМЕНИТЬ на реальный номер
  email: "info@rem-maxvich-stroi.ru", // ← ЗАМЕНИТЬ на реальный email
  address: "г. Нижний Новгород",      // ← ДОБАВИТЬ полный адрес
  telegram: "https://t.me/username",  // ← ЗАМЕНИТЬ на вашу ссылку
  whatsapp: "https://wa.me/...",      // ← ЗАМЕНИТЬ на вашу ссылку
  vk: "https://vk.com/username",      // ← ЗАМЕНИТЬ на вашу ссылку
}
```

### 2. Добавить фото владельца
Поместите фото в папку `public/images/` и обновите [src/components/sections/HeroSection.tsx](../src/components/sections/HeroSection.tsx#L101-L114)

Замените секцию с заглушкой на:
```tsx
<Image
  src="/images/owner.jpg"
  alt="Владелец компании"
  width={500}
  height={500}
  className="rounded-2xl shadow-2xl"
/>
```

### 3. Следующие секции для разработки

#### Секция "Услуги" (Приоритет 1)
Создать файл `src/components/sections/ServicesSection.tsx`:
- Вкладки (Ремонт под ключ, Инженерия, Отдельные работы)
- Карточки услуг с ценами
- Кнопки "Подробнее" и "Заказать"

#### Секция "Портфолио" (Приоритет 1)
Создать файл `src/components/sections/PortfolioSection.tsx`:
- Галерея работ до/после
- Фильтр по категориям
- Swiper.js для слайдера

#### Секция "Калькулятор" (Приоритет 1)
Создать файл `src/components/sections/CalculatorSection.tsx`:
- Форма с полями (площадь, тип помещения, вид ремонта)
- Расчет стоимости
- React Hook Form + Zod валидация

#### Секция "Отзывы" (Приоритет 2)
Создать файл `src/components/sections/ReviewsSection.tsx`:
- Карточки отзывов
- Видео-отзывы
- Слайдер Swiper.js

---

## 🛠 Команды для работы

### Запуск проекта:
```bash
npm run dev
```
Откройте http://localhost:3000

### Сборка проекта:
```bash
npm run build
npm start
```

### Проверка кода:
```bash
npm run lint
```

---

## 📁 Где что находится

```
src/
├── app/
│   ├── page.tsx          ← ГЛАВНАЯ СТРАНИЦА (добавляйте новые секции сюда)
│   ├── layout.tsx        ← Layout всего сайта
│   └── globals.css       ← Глобальные стили
├── components/
│   ├── ui/               ← Базовые компоненты (кнопки, карточки)
│   ├── layout/           ← Header, Footer
│   └── sections/         ← Секции страниц (Hero, Advantages, и т.д.)
├── data/
│   └── constants.ts      ← КОНТАКТЫ И КОНСТАНТЫ (обновить здесь!)
└── types/
    └── index.ts          ← TypeScript типы
```

---

## 🎨 Использование компонентов

### Кнопка:
```tsx
import { Button } from "@/components/ui/Button";

<Button variant="default" size="lg">
  Оставить заявку
</Button>
```

Варианты: `default`, `secondary`, `outline`, `ghost`, `link`
Размеры: `sm`, `default`, `lg`, `icon`

### Карточка:
```tsx
import { Card } from "@/components/ui/Card";

<Card hover>
  Содержимое карточки
</Card>
```

### Секция:
```tsx
import { Section } from "@/components/ui/Section";
import { Container } from "@/components/ui/Container";

<Section id="services">
  <Container>
    Ваш контент
  </Container>
</Section>
```

---

## 🎯 Цветовая схема

В Tailwind CSS используйте:
- `text-primary-500` - оранжевый текст
- `bg-primary-500` - оранжевый фон
- `text-secondary-500` - темный текст
- `bg-secondary-500` - темный фон
- `text-accent-500` - синий текст

---

## 📞 Нужна помощь?

1. Проверьте [документацию Next.js](https://nextjs.org/docs)
2. Изучите [Tailwind CSS](https://tailwindcss.com/docs)
3. Посмотрите готовые компоненты в `src/components/`
4. Следуйте плану разработки в [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)

---

**Удачи в разработке! 🚀**
