# Калькулятор стоимости ремонта

Модульный JavaScript компонент для расчета стоимости ремонта квартир, офисов и домов.

## 📁 Структура файлов

```
calculator/
├── config.js         # Конфигурация цен (можно редактировать через админ-панель)
├── template.js       # HTML-шаблон калькулятора
├── calculator.js     # Основная логика и API
├── calculator.css    # Стили компонента
├── demo.html         # Демо-страница с примером использования
└── README.md         # Документация
```

## 🚀 Быстрый старт

### 1. Подключение на страницу

```html
<!-- Подключите CSS -->
<link rel="stylesheet" href="calculator/calculator.css">

<!-- Создайте контейнер -->
<div id="myCalculator"></div>

<!-- Подключите JavaScript -->
<script src="calculator/config.js"></script>
<script src="calculator/template.js"></script>
<script src="calculator/calculator.js"></script>

<!-- Инициализируйте -->
<script>
  const calculator = initRepairCalculator('myCalculator');
</script>
```

### 2. Просмотр демо

Откройте файл `demo.html` в браузере, чтобы увидеть калькулятор в действии.

## 📊 Параметры расчета

Калькулятор учитывает следующие параметры:

1. **Площадь** (м²) - основной параметр для расчета
2. **Тип объекта** - Квартира / Офис / Дом
3. **Тип здания** - Новостройка / Вторичка
4. **Количество санузлов** - от 0 и более
5. **Тип отделки** - Косметический / Дизайнерский / Капитальный
6. **Электрика** - да / нет
7. **Теплый пол** - да / нет
8. **Сантехника** - да / нет
9. **Перепланировка** - да / нет

## 💰 Формула расчета

```
Итоговая стоимость = 
  (Базовая цена за м² × Площадь) ×
  Коэффициент типа объекта ×
  Коэффициент типа здания +
  (Количество санузлов × Цена за санузел) +
  Дополнительные услуги
```

## ⚙️ Конфигурация цен

Все цены хранятся в файле `config.js`:

```javascript
const CALCULATOR_CONFIG = {
  // Базовая цена за м²
  basePrice: {
    косметический: 5000,
    дизайнерский: 12000,
    капитальный: 20000
  },
  
  // Коэффициенты
  objectType: {
    квартира: 1.0,
    офис: 1.2,
    дом: 1.3
  },
  
  buildingType: {
    новостройка: 1.0,
    вторичка: 1.15
  },
  
  // Цена за санузел
  bathroomPrice: 80000,
  
  // Дополнительные услуги
  additionalServices: {
    электрика: { type: 'perSquareMeter', price: 2000 },
    теплыйПол: { type: 'perSquareMeter', price: 3500 },
    сантехника: { type: 'perSquareMeter', price: 1500 },
    перепланировка: { type: 'fixed', price: 150000 }
  }
};
```

## 🔧 API для админ-панели

### Получение конфигурации

```javascript
const config = calculator.getConfig();
console.log(config);
```

### Обновление цен

```javascript
calculator.updateConfig({
  basePrice: {
    косметический: 6000,
    дизайнерский: 14000,
    капитальный: 22000
  },
  bathroomPrice: 90000
});
```

### Пример интеграции с админ-панелью

```javascript
// Загрузка цен с сервера
async function loadPrices() {
  const response = await fetch('/api/calculator/prices');
  const prices = await response.json();
  calculator.updateConfig(prices);
}

// Сохранение цен на сервер
async function savePrices(newPrices) {
  const response = await fetch('/api/calculator/prices', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(newPrices)
  });
  
  if (response.ok) {
    calculator.updateConfig(newPrices);
    alert('Цены обновлены!');
  }
}
```

## 🎨 Кастомизация

### Изменение стилей

Калькулятор поддерживает CSS-переменные для быстрой кастомизации:

```css
.repair-calculator {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}
```

### Темная тема

Калькулятор автоматически поддерживает темную тему при добавлении класса `dark-theme` к `<body>`.

## 📱 Адаптивность

Калькулятор полностью адаптивен и корректно работает на всех устройствах:
- Десктоп (>768px)
- Планшеты (768px)
- Мобильные (<768px)

## 🔄 Автоматический пересчет

Калькулятор автоматически пересчитывает стоимость при изменении любого параметра после первого расчета.

## 📝 Примеры использования

### Базовая инициализация

```javascript
const calculator = initRepairCalculator('calculatorContainer');
```

### С кастомной конфигурацией

```javascript
const customConfig = {
  ...CALCULATOR_CONFIG,
  basePrice: {
    косметический: 7000,
    дизайнерский: 15000,
    капитальный: 25000
  }
};

const calculator = initRepairCalculator('calculatorContainer', customConfig);
```

### Использование класса

```javascript
const calculator = new RepairCalculator('calculatorContainer', CALCULATOR_CONFIG);
calculator.init();
```

## 🌐 Совместимость

- Современные браузеры (Chrome, Firefox, Safari, Edge)
- IE11+ (с полифиллами)
- Мобильные браузеры (iOS Safari, Chrome Mobile)

## 📄 Лицензия

Этот компонент разработан для проекта Maxvich-rem.

## 👥 Использование в команде

Этот калькулятор создан в ветке `gleb` и готов к интеграции другими разработчиками:

1. Просто подключите файлы калькулятора
2. Вызовите `initRepairCalculator('yourContainerId')`
3. Калькулятор автоматически создаст весь необходимый HTML

Не требуется создавать HTML вручную - всё работает через JavaScript!
