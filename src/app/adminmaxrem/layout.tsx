export const metadata = {
  title: 'Админ панель - MaxRem',
  description: 'Панель управления сайтом MaxRem',
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
